on:
  push:
    branches:
    - master
  release:
    types:
    - created
jobs:
  publish:
    if: success() && startsWith(github.ref, 'refs/tags/')
    steps:
      - name: deploy
        run: npm run deploy
    runs-on: ubuntu-latest
    env:
      VSCE_PAT: ${{ secrets.VSCE_PAT }}
